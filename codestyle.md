
# 前端代码规范 (JavaScript/Vue)

本项目代码规范主要基于 [Airbnb JavaScript Style Guide](https://github.com/airbnb/javascript)。

## 主要规则
* 变量：优先使用 `const`，其次 `let`，不使用 `var`。
* 命名：变量和函数名使用 `camelCase` 。
* 风格：在 `package.json` 中配置 Prettier 进行自动格式化。
